# Digital Clock

A polished digital clock web project with time-based greetings, changing backgrounds, and a neon glass UI.

## Files

- `DC.html` - main page for the clock.
- `flicker.css` - page styling and neon glass effects.
- `DC.js` - clock logic, live time updates, theme switching, and greetings.
- `config.js` - theme definitions, greetings, and background image references.
- `assets/` - static image assets used by the clock and the favicon.

## Usage

1. Open `digitalClock/DC.html` in a browser.
2. The clock updates every second.
3. The greeting changes based on the current time of day.
4. The background uses themed SVG images stored in `assets/`.

## Notes

- If you want to change the user name, update `CLOCK_CONFIG.username` in `config.js`.
- To change the greetings, edit the `greeting` values in `config.js`.
- To change the background images or theme styles, edit the `bgImage` values in `config.js`.
- `assets/` contains theme SVG images and the browser favicon.

## How to Customize

- `DC.html` is the page you open in a browser.
- `flicker.css` controls the clock layout, glass effect, colors, and fonts.
- `DC.js` contains the clock update logic, greeting application, and theme switching.
- `config.js` defines the theme entries and image sources used by `DC.js`.
- `assets/` stores the image files referenced in `config.js`.

### Recommended edits

- Update `CLOCK_CONFIG.username` to change the displayed name.
- Replace `assets/*.svg` with your own images for custom backgrounds.
- Adjust `flicker.css` if you want a different font size, card color, or button style.
- Use your browser inspector to quickly test CSS changes live.

## Suggested image sources

🌅 Morning
- Unsplash Morning Backgrounds – Free, high-quality sunrise and morning nature images
- Pexels Morning Photos – Large library of morning sun and sky images

☀️ Afternoon
- Unsplash Afternoon Pictures – Bright daylight and afternoon sky shots
- Pexels Afternoon Backgrounds – Free afternoon scenes and landscapes

🌇 Evening
- Unsplash Evening Pictures – Sunset and dusk images with warm tones
- Pexels Evening Backgrounds – Free evening sky and cityscape photos

🌌 Night
- Unsplash Night Pictures – Night sky, stars, and city lights
- Pexels Night Photos – Free night backgrounds including galaxies and urban lights
"# erina" 
